- **Scatter search's noise handling defers an undecided contest for three draws, not five
  (#663, ADR-0141).** `ss_noise_max_draws` now defaults to 3. A conf that sets
  `ss_noise_max_draws` itself is unchanged.
